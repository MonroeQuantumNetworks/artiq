# conda description for an environment to install ARTIQ without dev support
# $ conda env create -f conda/artiq.yaml
name: artiq
channels:
  - m-labs/label/main
  - defaults
  - conda-forge/label/main
dependencies:
  - artiq
